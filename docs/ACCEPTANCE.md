# Capability acceptance record

This record defines reviewable completion criteria for the supported product: an NSE equity bot with intraday long/short strategies and optional long delivery/swing trading through Kite Connect. Demonstrated profitability is outside this work. A count of indicators or patterns is not a quality score, and this scope does not claim every possible trading strategy, derivatives, or exchange co-location trading.

| Area | Required evidence |
| --- | --- |
| Strategy decisions | Coordinated breakout, pullback, range reversion, opening range/drive, gap continuation/reversal, VWAP reclaim/rejection, squeeze, and relative strength; completed higher timeframe context; deterministic explanations and directional exits. |
| Data integrity | Causal completed candles; no future context; explicit warmup, missing data, stale quotes and market-context coverage; rate limited history and feed recovery. |
| Execution | Journal before broker I/O; long/short cover ownership and signed broker reconciliation; partial fills, rejections, ambiguous acknowledgements and cancellation races; no automatic duplicate submission. |
| Account risk | Current available funds, existing holdings and manual exposure; stock/gross/stress/correlation caps; loss limits/cooldown; liquidity and earnings-event entry gates. |
| Recovery | Restart reauthenticates the account, reconciles real orders and holdings, restores monitoring, and reanalyses current completed data before new entries. |
| Research | Shared decision logic; next-bar fills; explicit fees/slippage, gaps, short cash reservations and unresolved positions; repeatable bounded background research. |
| Operations | Authenticated responsive dashboard, live decisions/account/logs, operational readiness, portable setup, shutdown/backup guidance, and meaningful automated verification. |
| Broker integration | Read-only authenticated rehearsal, live feed and account reconciliation, and user-operated controlled execution verification against the real account. |

Implementation and test results are recorded below as work completes. Public documentation and mocks cannot substitute for broker integration evidence. No real orders are submitted as part of development verification.

## Verification on 17 September 2026

- `npm test`: 558 unit/integration checks passed on Windows with Node 24.9.0; zero failures and one file-symlink test skipped because this Windows session lacks the creation privilege. Coverage includes 11 directional intraday families, shared swing entry/exit rules, malformed/future candles, partial fills, ambiguous acknowledgements, signed position recovery, risk limits, official-feed schemas, worker deadlines, authorization, authenticated HTTP flows and paired backup integrity. POSIX permission assertions still require Linux/macOS execution.
- `npm run test:browser`: all 14 desktop/mobile Chromium checks passed (four dashboard checks and ten paper-mode checks). They exercise actual local login, dashboard state, settings, research, short-position rendering, logout, both paper/live setting transitions, current versus unsaved mode labels, restart locks across tabs, preservation of disabled live execution and rejection of changes while trading. Screenshots were visually inspected, including the mobile Save button clearing the fixed navigation. Tests use synthetic account data and never send real orders.
- `npm run benchmark`: 9,000 synthetic symbol analyses, all valid, in 5.293 seconds; 188 concurrent analytics workers; 4.843 GiB peak process memory; zero broker requests. This is one synthetic workload on this machine, not an exchange latency or throughput guarantee.
- Public read-only checks validated all eight configured official index constituent files and both NSE corporate-event feeds. A verified corporate-action dummy is excluded with a visible count; non-EQ index constituents are classification records, not automatic trade eligibility.
- Windows capacity detection verified 96 physical/192 logical processors while Node reported 64. The wider Windows scheduling estimate is used only on supported builds without a detected primary-group restriction. Linux/macOS restricted-capacity behavior is covered by injected tests; those operating systems have not been run here.
- Production dependency audit reported zero known vulnerabilities. Changes remain uncommitted and no deployment folder was introduced.
- Separate agent reviews identified and verified fixes for delayed benchmark-context signals, superseded queued candidates, conflicting swing entry/exit rules, contradictory broker acknowledgements and publicly served data/backup paths. The regression suite covers these findings. Setup also creates private POSIX credential files, and backup rejects mismatched process-environment credentials. This review is not an external security certification.
- Follow-through recovery checks verify that unusable daily-history responses and prior-session seed caches recover through validated downloads and bounded retries instead of remaining stuck. Feed-worker failure and exhausted SDK reconnection restore the same subscription with bounded backoff; concurrent restart/close requests retain the latest generation and cannot exceed three live workers. Unconfirmed worker retirement prevents a replacement until exit is established.
- Two actual process-crash tests force-terminate a child during an accepted-but-unacknowledged synthetic BUY or SELL. The unclosed WAL journal retains the order reservation; the operating system releases the process lock; actual engine reconnection discovers ownership by tag; partial/full exit P&L remains idempotent across restarts. No external connections or real orders are used, and physical power loss is not simulated.
- Cross-platform CI is prepared in [node-checks.yml](../.github/workflows/node-checks.yml) for pull requests and manual runs: Node 24.9.0 on Ubuntu, Windows and macOS, locked dependency installation, unit/integration checks and Chromium desktop/mobile checks. Official actions are pinned to verified commit hashes, repository access is read-only, and checkout credentials and automatic package-manager caching are disabled. The workflow has not been executed; its presence is not evidence that these operating-system checks passed.
- Actual authentication exposed a universe-selection defect: Kite returned 10,111 NSE cash instruments marked instrument type EQ, exceeding the 9,000 feed capacity. The corrected universe uses the official stock and ETF directories instead of treating this broad instrument type as stock eligibility. Public-source checks found 2,577 stock-directory records (2,309 EQ, 241 BE, 27 BZ) and 350 ETFs; all 2,659 entry-eligible records matched Kite. No arbitrary truncation was used. Tests also cover managed exceptions, directory outages/retry, same-day cache completeness and day rollover.
- Portfolio regression checks now include unfilled manual buys and shorts, partial fills, cancel/modify-pending commitments, invalid or unpriced order risk, and journal-owned protection/exit deduplication. A known broker order ID must match before journal ownership can release a reservation. Research and recovery checks cover unusable cache recovery, symbol-bound historical caches, superseded history/feed callbacks, retry after stream setup failure, missing sector history and exclusion of recovery-only instruments from sampling.
- Funding automation checks exercise zero-cash Start followed by a verified deposit in paper and live fixtures. Funding does not re-arm manual pauses, risk halts, restarted engines or day rollover. Flat live deposits/withdrawals preserve realised results and loss history, malformed balances cannot erase the budget, and a loss already at its limit halts before a larger deposit can change the threshold.
- Deterministic scheduler checks exercise funds/universe readiness, day changes, persistent capped retry delays, long outages, cancellation, partial completed reports, stale response rejection and shutdown without overlapping work. The Research page exposes the next retry and permits cancelling a pending retry. These checks use synthetic broker history.
- Activity polling checks reproduce the former loss of 150 events in a 250-event burst, then verify contiguous fixed-size pages and deduplication across overlapping stream, polling and manual refresh responses. The browser retains its latest 500 events; full export remains available for the older journal.
- Error-diagnostic regressions check broker failure categories, HTTP status and operation/stage while rejecting arbitrary error names and secret-bearing exception text. These are bounded diagnostic fields, not copies of broker responses.
- Timestamp regressions reject out-of-order quote updates before changing depth or position marks and deduct transit delay from the shared ten-second freshness lifetime. Clock-health tests cover latency bounds, positive/negative skew, invalid/missing headers, ambiguous responses, staleness, host-clock jumps and recovery. Entry and UI checks verify explicit clock blocking without rearming a manual pause.
- Holdings checks distinguish unsupported exchanges/instruments, missing eligibility, unavailable history and missing fresh quotes from completed hold/exit analysis. Snapshots are read-only, use current symbol/token identities and expose exit-only recovery scope without granting new holding permissions.
- Further regressions reproduced cumulative-volume rollback double counting, stale resource measurements after a backward clock correction, and lost ownership when a terminal broker response omitted its fill count. The fixes reject regressing volume before quote/candle mutation, use monotonic resource sampling, and retain ownership/reservations until valid cover or delivery fill data is reconciled. Coverage includes explicit and GTT exits, invalid legacy terminal caches, legitimate partial fills, zero-fill cancellations, and recovery without rearming entries. These are synthetic broker checks; no real orders were placed.

## Authenticated read-only verification

Times in this section are host-recorded IST. The later clock check below found that this host was about 223 seconds behind the broker. Earlier small counts of quotes that passed the host-relative freshness check do not establish actual current market-data coverage.

At 10:05 IST on 17 September 2026, a bounded read-only diagnostic used the existing encrypted session, verified the configured account identity, and downloaded the Kite NSE master. The new classifier selected 2,309 EQ-series stocks and 350 ETFs with zero missing directory matches. One quote request succeeded, one five-minute history request returned 73 candles, and a 15-second feed check observed packets for all 2,659 subscribed tokens. Receipt of a packet is not proof of a fresh executable quote for every security.

At 10:07 IST, the actual revised `TradingEngine.connect()` path was exercised with isolated in-memory state, paper mode, entries paused, and broker transport restricted to GET requests. It reported connected/monitoring, 2,659 subscriptions, fresh account/feed data and recovery ready. After the history-identity and stream-retry fixes, that revision was checked again at 10:16 IST with the same connection and recovery results. Twenty-one token quotes met the engine's freshness checks within the final five-second observation; no claim of fully warmed or simultaneously executable universe coverage is made. Each engine check issued seven broker GET requests, no attempted mutation requests, and created zero bot positions or intents. All diagnostic streams were closed after verification; the user's running server and account were not restarted or modified. Later funding, research-scheduling and activity-polling changes are covered by the local regression checks, not by these earlier broker observations.

These checks establish current read access and a working connection path. They do not prove sustained trading, broker order acceptance, actual fills or protection, or real-position recovery after disruption.

At host 10:37:58 IST, a 60-second GET-only/in-memory observation received 35,226 tick packets across all 2,659 subscriptions. Of these, 35,018 were more than ten seconds ahead of the host; only 16 tokens initially matched its clock and fresh coverage fell to zero after 20 seconds. A second observation found Kite's HTTPS Date ahead by 223.102 seconds and median exchange packet time ahead by 222.047 seconds. Windows Time reported not synchronized, stratum zero, no successful synchronization and Local CMOS Clock as source; its configured time.windows.com peer was pending. This is a verified environment blocker, not a reason to reduce the freshness requirement.

At host 10:47:05 IST, the code with clock protection repeated the authenticated connection in isolated paper state with entries paused. It observed a broker-clock offset interval of 221.288 to 223.357 seconds, reported `system_clock_skew`, and accepted zero quotes despite receiving packets for the whole universe. The probe issued seven GET requests, attempted zero mutations and created no positions or intents. All diagnostic streams were closed. System clock synchronization was requested for user confirmation because it changes time for every application; no system time, time-service configuration or production journal was changed.

## External checks still required

Local credentials and a current authenticated session are now present; authenticated reads and subscriptions work. Correct the host clock and repeat freshness/coverage checks before claiming live data readiness. Live order permission, outbound-IP enforcement, cover/GTT acceptance, actual partial fills, delivery authorization and reconciliation of real managed exposure after interruption remain unverified. No real orders were placed. Actual execution verification requires a user-operated controlled test. Read-only evidence cannot justify claiming these checks passed or assigning a verified 10/10 score.

Cross-platform test execution, sustained market-session operation, network/power interruption behavior against the real broker, and independent security/operational review remain outside the evidence collected on this machine. These are non-profitability verification gaps, not demonstrated profitability requirements.

The local cross-platform environment was rechecked: Docker's service and both Docker WSL distributions remain stopped. No Linux/macOS runtime test was claimed, and Docker configuration, services and existing workloads were left unchanged. The earlier absence of local credentials/configuration/journal is superseded by the authenticated checks above. The current user server still needs a restart to load these source changes.
